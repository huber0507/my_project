#ifndef __BSP_BEEP_H  // 头文件卫士：判断宏 __BSP_BEEP_H 是否未被定义
#define __BSP_BEEP_H  // 若未定义，则定义该宏，标记头文件已包含，避免重复引入

/*
 * 引入 imx6ul.h 头文件
 * 作用：提供 I.MX6ULL 芯片的底层硬件定义，包括：
 *       1. GPIO 模块寄存器结构体（如 GPIO_TypeDef）及基地址（如 GPIO5）；
 *       2. IOMUXC 复用控制相关的宏定义（如 IOMUXC_SNVS_SNVS_TAMPER1_GPIO5_IO01）；
 *       3. 芯片外设的地址映射与位掩码，是驱动代码操作硬件的基础。
 * 备注：该文件由芯片厂商提供或根据参考手册生成，是裸机开发的核心依赖。
 */
#include "imx6ul.h"

/*
 * 蜂鸣器相关宏定义：预留的蜂鸣器标识/状态宏
 * 定义：#define BEEP 0
 * 用途：当前取值为 0，可根据需求扩展为：
 *       - 蜂鸣器引脚标识（如对应 GPIO5_IO01 的引脚编号，便于统一管理）；
 *       - 蜂鸣器默认状态（如初始关闭状态码）；
 *       - 多蜂鸣器系统中的设备编号（单蜂鸣器场景暂作预留）。
 */
#define BEEP	0

/*
 * 函数声明：蜂鸣器初始化函数
 * 功能：完成蜂鸣器对应引脚的硬件初始化，包括：
 *       1. IO 复用配置（将引脚功能切换为 GPIO）；
 *       2. IO 电气属性配置（下拉、驱动能力、速度等）；
 *       3. GPIO 方向设置（输出模式）及初始电平配置（默认关闭蜂鸣器）。
 * 参数：无
 * 返回值：无
 * 实现位置：对应 bsp_beep.c 文件中的同名函数
 */
void beep_init(void);

/*
 * 函数声明：蜂鸣器开关控制函数
 * 功能：通过修改 GPIO 输出电平，控制蜂鸣器的打开/关闭状态。
 * 参数：status - 控制状态（需配合外部定义的 ON/OFF 宏使用，通常：
 *               ON=1 表示打开蜂鸣器，OFF=0 表示关闭蜂鸣器）。
 * 返回值：无
 * 实现位置：对应 bsp_beep.c 文件中的同名函数
 * 备注：若未定义 ON/OFF 宏，需在本头文件或全局头文件中补充（如 #define ON 1 #define OFF 0）。
 */
void beep_switch(int status);


#endif  // __BSP_BEEP_H  // 结束头文件卫士，与开头 #ifndef __BSP_BEEP_H 配对